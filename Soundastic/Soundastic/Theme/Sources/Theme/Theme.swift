import SwiftUI

public enum Theme {
  public enum Style {
    public enum Button {
      case main
    }
  }

  public enum Color {
    public enum Background {
      case main
      case outline
    }

    public enum Text {
      case main
    }
  }
}
